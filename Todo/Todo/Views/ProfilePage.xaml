<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Todo.Views.ProfilePage">

    <TableView Intent="Settings" HasUnevenRows="True">
        <TableSection Title="Successed Jobs">
            <ViewCell>
                <Button Text="Archive"
                        CornerRadius="10"
                        Margin="5"/>
            </ViewCell>
        </TableSection>

        <TableSection Title="Statistics">
            <ViewCell>
                <StackLayout Margin="10" Spacing="5">
                    <StackLayout.Resources>
                        <ResourceDictionary>
                            <Style TargetType="Label">
                                <Setter Property="FontSize" Value="16"/>
                            </Style>
                        </ResourceDictionary>
                    </StackLayout.Resources>
                    
                    <Label Text="{Binding ActiveJobs, StringFormat='Active: {0}'}"/>
                    <Label Text="{Binding EndedJobs, StringFormat='Ended: {0}'}"/>
                    
                </StackLayout>
            </ViewCell>
        </TableSection>
    </TableView>

</ContentPage>